# External discovery is enabled by default. Set to 0 for an immediate operational rollback.
PAPERWORDS_EXTERNAL_DISCOVERY_ENABLED=1

# Optional Crossref polite-pool contact. No API key is required.
# PAPERWORDS_CROSSREF_MAILTO=maintainer@example.com
