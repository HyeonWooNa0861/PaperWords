# PaperWords MVP Ultragoal execution brief

Implement and locally verify the approved PaperWords MVP in `/Users/nahw/Documents/PaperWords`. The authoritative planning inputs are `.omx/plans/prd-paperwords-mvp.md`, `.omx/plans/test-spec-paperwords-mvp.md`, and `.omx/plans/ralplan-consensus-paperwords-mvp.md`.

Codex owns implementation and completion. Claude is a bounded read-only advisor at the architecture, representative UI, initial three-term content, and release gates. Runtime, build, and default tests are network-independent. Do not use credentials, paid services, commit, push, deploy, or publish externally. Preserve the fully populated 90-day KST schedule with its versioned 20-day no-repeat window and the provenance-versus-truth boundary.

Execution is sequential and solo by default to limit coordination and token overhead. Native specialist agents are reserved for the required independent review and verification gates or a genuinely bounded blocker. Every story must return changed files, fresh validation output, residual risks, and the next story before checkpointing.
